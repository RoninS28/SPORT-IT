<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.temp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Temp">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/Theme.Temp.NoActionBar" />
        <activity
            android:name=".ViewTeams"
            android:label="@string/title_activity_view_teams"
            android:theme="@style/Theme.Temp.NoActionBar" />
        <activity android:name=".SinglePlayer" />
        <activity android:name=".Create_Profile" />
        <activity android:name=".Create_Profile_Team" />
        <activity android:name=".Create_Complete_Profiles" />
        <activity android:name=".Login_Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>